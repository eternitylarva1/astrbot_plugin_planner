{
  "schedule_api_base": {
    "description": "Schedule App 后端 API 地址",
    "type": "string",
    "default": "http://localhost:8080",
    "hint": "填写 Schedule App 后端服务的地址，如 http://localhost:8080"
  },
  "frontend_url": {
    "description": "Schedule App 前端地址（用于截图）",
    "type": "string",
    "default": "http://localhost:8080",
    "hint": "填写前端服务地址，截图功能会加载此页面"
  },
  "enable_screenshot": {
    "description": "是否启用截图功能",
    "type": "bool",
    "default": true,
    "hint": "关闭后可减少资源消耗"
  }
}
